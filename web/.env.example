# Copy to .env and fill in.
# A long, random secret used to sign tokens/cookies.
PAYLOAD_SECRET=replace_with_a_long_random_string

# Database. Leave blank for local dev (uses a SQLite file: firstchoice.db).
# In production, set the Postgres connection string from the server.
DATABASE_URI=
